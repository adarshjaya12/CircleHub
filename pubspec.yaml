name: circle_hub
description: "Circle Hub — Smart Alarm for 1080x1080 circular display"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  http: ^1.3.0
  intl: ^0.20.2
  shared_preferences: ^2.3.3
  cached_network_image: ^3.4.1
  xml: ^6.5.0
  weather_icons: ^3.0.0
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
